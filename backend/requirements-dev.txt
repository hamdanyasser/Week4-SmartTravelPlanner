# Developer-only dependencies. Not installed in the production image.
# Install with:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# --- Linters / formatters ---
ruff==0.7.4
black==24.10.0
pre-commit==4.0.1

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.27.2

# --- Optional experiment tracking ---
mlflow==2.17.2
