# --- Day 1 deps ---
# Pinned for reproducibility. Each new dep gets a one-line justification
# in CODE_REVIEW_NOTES.md when it lands.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Day 2 ML deps ---
# pandas: CSV load + tabular handling for the destinations dataset.
# scikit-learn: Pipeline, StandardScaler, the 3 classifiers, k-fold CV, GridSearchCV.
# joblib: persists the trained model artifact (model.joblib).
pandas==2.2.3
scikit-learn==1.5.2
joblib==1.4.2

# --- Day 2 RAG / DB deps ---
# SQLAlchemy: async ORM/session foundation for Postgres-backed app data.
# asyncpg: async Postgres driver used by SQLAlchemy.
# pgvector: SQLAlchemy Vector type and cosine-distance helpers.
SQLAlchemy==2.0.36
asyncpg==0.30.0
pgvector==0.3.6

# --- Day 3 backend deps ---
# bcrypt: password hashing for local auth.
# PyJWT: signed JWT access tokens.
# httpx: async HTTP client for weather/webhook calls.
# tenacity: async retry/backoff for webhook delivery.
# langgraph: small explicit graph for the three-tool agent flow.
bcrypt==4.2.1
PyJWT==2.10.1
httpx==0.27.2
tenacity==9.0.0
langgraph==0.2.60

# --- Day 4 deps ---
# alembic: schema migrations versioned in `backend/alembic/versions/`.
alembic==1.14.0
